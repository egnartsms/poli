img2fs
   dumpModule
   genModuleImportsSection
   ind
   sortedImportsInto
   writingToStream
run
   dumpModuleImportsSection
   handleOperation
   serialize
-----
sayhello ::= function () {
   console.log('Hello!');
}
add ::= function (x, y) {
   return x + y;
}
multiply ::= function (x, y) {
   return x * y;
}
compareImports ::= 0
