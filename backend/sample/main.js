// const fearNot = nonExistingVariable;

const x0 = 0;

// Slightly bigger than 'x0'
const x1 = x0 + 20;
/**
 * Here follows a multiline docstring comment.
 */

const sum = (x0 + x1) * 2;

duck;

/* Backing
  facking
  style
  */

// function sample() {
//    return [x0, x1];
// }
